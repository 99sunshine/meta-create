import React from 'react';
import ReactDOM from 'react-dom/client';
import MetaCreateApp from '../MetaCreate_Prototype.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <MetaCreateApp />
  </React.StrictMode>
);
